'use strict';

function double_to_one(collection) {
  var one_array=[];
  for(var i=0;i<collection.length;i++){
    if(Array.isArray(collection[i])==true){
       for(var j=0;j<collection[i][j];j++){

       }
    }
  }
  //在这里写入代码
}

module.exports = double_to_one;
